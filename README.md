# 跨校支教轮岗调度

本项目记录县域教育共同体的排程约定。`fixtures/rotation-context.json` 展示教师资质、校区需求和一段路程信息，`src/domain.js` 统一计划阶段及中断原因。所有时间均采用带偏移量的 ISO 8601 字符串，路程分钟数表示校门到校门的常规耗时。

请使用 Node.js 20 或更高版本，执行 `npm test` 校验资料格式。各学校的实际人员信息与连接配置只能保存在受控环境中。
